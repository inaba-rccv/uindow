{
  "name": "@uindow/monorepo",
  "type": "module",
  "version": "1.0.0",
  "packageManager": "pnpm@10.9.0",
  "description": "",
  "author": {
    "name": "Inaba"
  },
  "scripts": {
    "dev": "pnpm --filter @uindow/docs dev",
    "build": "pnpm -r --filter=./packages/** --parallel run build",
    "build:types": "pnpm --filter @uindow/types run build",
    "build:lib": "pnpm --filter uindow build",
    "publish": "pnpm --filter uindow publish",
    "build:docs": "pnpm --filter @uindow/docs build",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@vueuse/core": "^13.1.0",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^4.13.2",
    "@types/node": "^24.0.4",
    "@uindow/components": "workspace:*",
    "@uindow/types": "workspace:*",
    "esbuild": "^0.25.4",
    "eslint": "^9.27.0",
    "minimist": "^1.2.8",
    "typescript": "^5.8.3"
  }
}
